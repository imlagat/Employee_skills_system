SECRET_KEY=django-insecure-dev-key-change-in-production-%h%1o=99$p=y%956y
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Postgres (used when USE_POSTGRES=True)
USE_POSTGRES=False
DB_NAME=skillsystem
DB_USER=skillsystem_user
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432

# Celery / Redis
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# CORS - React dev server
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Email SMTP Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=superposlish@gmail.com
# ----------------------------------------------------
# ACTION REQUIRED: You must generate an App Password in your Google Account
# and paste it below instead of 'your-app-password'.
# A standard email password will not work!
# ----------------------------------------------------
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=superposlish@gmail.com
